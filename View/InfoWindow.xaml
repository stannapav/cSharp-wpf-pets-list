<Window x:Class="finalProject.View.InfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:finalProject.View"
        mc:Ignorable="d" WindowStartupLocation="CenterOwner"
        Title="InfoWindow" Height="350" Width="350">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Info" FontSize="20" HorizontalAlignment="Center" Margin="0,25,0,0"/>
                <TextBlock Name="tbInfo" Text="{Binding AnimalInfo}" FontSize="17" HorizontalAlignment="Center"/>
                <Button Name="btnPutInFile" Width="110" Height="30" Margin="10" Content="Put info in a file" FontSize="15" Click="btnPutInFile_Click"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>